import { createElement } from 'react';
import styles from './TableSuffix.pcss';


const TableSuffix = ({ children }) => (
  <div className={styles.TableSuffix}>{children}</div>
);


export default TableSuffix;
