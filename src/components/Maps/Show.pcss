@import "../../lib/variables";


.title {
  font-size: 2.25rem;
  font-weight: 300;
  margin-top: grid(2);
}


.url {
  font-size: .9em;
  margin-bottom: grid(3);

  & a {
    color: $blue;
    font-weight: 600;
    letter-spacing: 0.0125em;
    text-decoration: none;
  }
}
