@import "../lib/variables";


html {
  font-size: 16px;
}

body {
  background-color: $light;
}

.App {
  color: $dark;
  font-family: $default-font;
  font-size: 1rem;
  line-height: 1.7;
}

.wrapper {
  margin-bottom: grid(7);
}

a {
  color: inherit;
  cursor: pointer;
}

h2 {
  font-weight: 300;
  opacity: .75;
}
